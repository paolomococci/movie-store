<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base -->
    <style name="Theme.MovieStoreRetriever" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize -->
    </style>

    <style name="Theme.MovieStoreRetriever.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MovieStoreRetriever.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MovieStoreRetriever.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
